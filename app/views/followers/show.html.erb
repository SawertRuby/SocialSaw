<%# app/views/profiles/show.html.erb %>

`   `

<h2>Подписчики</h2>
<ul>
    <% if @profile.followers.count == 0 %>
        <p>Нет подписчиков</p>
    <%else%>
        <% @profile.followers.each do |follower| %>
            <li><%= link_to follower.username, follower %></li>
        <% end %>
    <% end %>
</ul>

<h2>Подписки</h2>
<% if @profile.following.count == 0%>
    <p>Нет подписок</p>
<% else %>
    <ul>
        <% @profile.following.each do |followed| %>
            <li><%= link_to followed.username, followed %></li>
            <% if user_signed_in? %>
                <%= button_to 'Unfollow', profile_followers_path(followed), method: :delete, class: 'btn btn-secondary' %>
            <% end %>

        <% end %>
    </ul>
<% end %>